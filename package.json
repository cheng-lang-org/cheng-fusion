{
  "name": "cheng-fusion-mcp",
  "version": "1.0.0",
  "description": "Standalone MCP server exposing deterministic Cheng compiler fusion tools (CSG, LSP, crash triage, line maps, profiling, symbol regression checks) over stdio JSON-RPC.",
  "type": "module",
  "main": "index.ts",
  "bin": {
    "cheng-fusion-mcp": "index.ts"
  },
  "scripts": {
    "start": "bun run index.ts"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
