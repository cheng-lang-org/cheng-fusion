import std/strings as strings

fn fixedCount(parts: str[2]): int32 =
    return parts.len

fn makeText(value: int32): str =
    return strings.IntToStr(value)

fn main(): int32 =
    if fixedCount([makeText(8), "suffix"]) != 2:
        return 1
    return 0
