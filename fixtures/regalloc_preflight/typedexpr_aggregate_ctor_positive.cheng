type
    Box =
        title: str
        count: int32

fn consume(value: Box): int32 =
    return value.count + value.title.len

fn main(): int32 =
    if consume(Box(title: "x", count: 41)) != 42:
        return 1
    return 0
