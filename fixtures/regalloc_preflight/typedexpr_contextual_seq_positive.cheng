import std/strings as strings

fn dynamicCount(parts: str[]): int32 =
    return parts.len

fn makeText(value: int32): str =
    return strings.IntToStr(value)

fn main(): int32 =
    if dynamicCount([makeText(7), "suffix"]) != 2:
        return 1
    return 0
