var gLine: str
var gOut: str

fn takeLine(out: var str, line: str) =
    if len(out) > 0:
        out = Fmt"{out}\n"
    out = Fmt"{out}{line}"

fn main(): int32 =
    gLine = "hello"
    gOut = ""
    takeLine(gOut, gLine)
    if len(gOut) != 5:
        return 1
    return 7
