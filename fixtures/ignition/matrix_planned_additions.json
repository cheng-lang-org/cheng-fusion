[
  {
    "name": "ctor-positional-real-emit",
    "tags": [
      "family:ctor-position",
      "kind:positional"
    ],
    "fixture": "ctor-positional-real-emit.cheng",
    "expectCompileBail": 6641,
    "note": "as-measured@DRV78 (2026-07-12, /Users/lbcheng/cheng-f24/DRV78): compile rc=2, ZC_NOT_READY bail=6641 (idx=0/1 function=main body_kind=statement_sequence line=7) -- same honest diagnosed-reject contract as fam_7_positional, now probed with two same-type int32 fields (Pair(3,5)) so ordering cannot be inferred from field type, a stronger test of the still-unimplemented 'map positional args by declaration order to a real emit' goal named in form41 follow-up (docs/op-lane-queue-2026-07-09.md:712-713). Gold-compare: cheng.stage3 (cold compiler) also rejects this source, compile rc=2 with diagnostic 'unresolved function call Pair' -- the cold compiler has no positional-constructor-call syntax support at all, a different code path than DRV78's ZC_NOT_READY poison but the same outcome (no valid emit exists anywhere for this shape). Consistent, non-regressed coverage gap; no discrepancy between gold and DRV78 to reconcile."
  },
  {
    "name": "global-init-str-field",
    "tags": [
      "family:ctor-position",
      "kind:global"
    ],
    "fixture": "global-init-str-field.cheng",
    "expectRc": 1,
    "note": "as-measured@DRV78 (2026-07-12): compile rc=0, run rc=1 (len(g) != 7 branch taken, i.e. module-level `var g: str = \"s.cheng\"` combined decl+init reads back as an empty/zero-length string at runtime) -- reproduces form53 (docs/op-lane-queue-2026-07-09.md:814): 'declare+init combined' loses the initializer for bare module-level str globals, same family as fam_5_global's struct-field case but isolated to a plain str global with no constructor call involved. Gold-compare: cheng.stage3 (cold compiler) shows the identical wrong answer, compile rc=0 / run rc=1 -- this is a real, non-regressed, pre-existing language-level defect present in both the production driver and the cold golden compiler, not a DRV78-specific regression. Recorded as-measured (rc=1, i.e. the known-broken state), not force-fixed; this is the hardening/coverage-lock value per form53's disposition (queued as a matrix hardening item, not a firing blocker)."
  },
  {
    "name": "assign-position-ctor-707",
    "tags": [
      "family:ctor-position",
      "kind:assign"
    ],
    "fixture": "assign-position-ctor-707.cheng",
    "expectRc": 7,
    "note": "as-measured@DRV78 (2026-07-12): compile rc=0, run rc=7 (correct) for `var x: Loc; x = Loc(name: \"s.cheng\", num: seven())` -- a named-field ctor at assign position (not let-binding) where one field value is itself a nested function call, the shape adjacent to form42's LetCall-only fix (docs/patches-form42-ctor-callvalue.patch fixes only the co-declared `var x = T(...)` LetCall path; assign position was named as still carrying the pre-existing hard bail=707 in docs/op-lane-queue-2026-07-09.md:713 'residual: assign/argpos/return position ctor = existing 707/44/801 hard-bail family'). Also cross-checked a second variant routing the aggregate through a plain helper call (`x = build()` where build() returns Loc, matching the more literal 707 description 'aggregate-LHS-assign with RHS call' from docs/beat-c-zero-c-clearing-queue-2026-07-04.md:116) -- also rc=7 on DRV78. Gold-compare: cheng.stage3 gives rc=7 on both variants too, matching DRV78 exactly. Honest disclosure: the historically-cataloged 707 hard-bail residual for this exact shape family was NOT reproduced with either construction tried here; both DRV78 and gold agree and are correct, so either the residual described in the 07-11 queue note has since been closed by later op-lane commits (many form4x/5x fixes landed same day/next day per the queue), or its true trigger condition differs from what these two fixtures construct. Recorded as a currently-passing regression lock for the assign-position-ctor family, not a reproduced defect; flagging the doc-vs-measurement discrepancy for follow-up rather than asserting a fix that wasn't verified against the original trigger."
  },
  {
    "name": "argpos-ctor-44",
    "tags": [
      "family:ctor-position",
      "kind:argpos"
    ],
    "fixture": "argpos-ctor-44.cheng",
    "expectRc": 7,
    "note": "as-measured@DRV78 (2026-07-12): compile rc=0, run rc=7 (correct) for `take(Loc(name: \"s.cheng\", num: seven()))` -- named-field ctor at function-argument position where one field value is a nested call, adjacent to form42's LetCall-only fix; docs/op-lane-queue-2026-07-09.md:713 names 'argpos ctor' as carrying a pre-existing hard bail=44 not covered by that fix. Also cross-checked a helper-wrapped variant `take(build())` (build() returns Loc) -- also rc=7 on DRV78. Gold-compare: cheng.stage3 gives rc=7 on both variants, matching DRV78 exactly. Honest disclosure: same as assign-position-ctor-707 -- the historically-cataloged 44 hard-bail for this position/shape family was NOT reproduced with either construction; both compilers agree and are correct. Either later op-lane work already closed it or the true trigger differs from these fixtures. Recorded as a currently-passing regression lock for the argpos-ctor family; doc-vs-measurement gap flagged, not silently asserted as fixed."
  },
  {
    "name": "returnpos-ctor-801",
    "tags": [
      "family:ctor-position",
      "kind:returnpos"
    ],
    "fixture": "returnpos-ctor-801.cheng",
    "expectRc": 7,
    "note": "as-measured@DRV78 (2026-07-12): compile rc=0, run rc=7 (correct) for `fn make(): Loc = return Loc(name: \"s.cheng\", num: seven())` -- named-field ctor at return position where one field value is a nested call, adjacent to form42's LetCall-only fix; docs/op-lane-queue-2026-07-09.md:713 names 'return-position ctor' as carrying a pre-existing hard bail=801 not covered by that fix. Also cross-checked a double-wrapped variant (`fn build(): Loc = return Loc(...)` then `fn wrap(): Loc = return build()`) -- also rc=7 on DRV78. Gold-compare: cheng.stage3 gives rc=7 on both variants, matching DRV78 exactly. Honest disclosure: same pattern as the assign/argpos siblings -- the historically-cataloged 801 hard-bail for this position/shape family was NOT reproduced with either construction; both compilers agree and are correct. Either later op-lane work already closed it or the true trigger differs from these fixtures. Recorded as a currently-passing regression lock for the returnpos-ctor family; doc-vs-measurement gap flagged for follow-up, not silently asserted as fixed."
  },
  {
    "name": "t52a-return-call-double-exec",
    "tags": [
      "family:t52-return-call-fact-dedup",
      "kind:red-green-repro"
    ],
    "fixture": "t52a_return_call_double_exec.cheng",
    "expectRc": 1,
    "note": "as-measured (2026-07-15) three-way: oracle cheng.stage3 rc=1 (correct); defective DRV (scratchpad/chain24/ignite_20260714T1600/DRV, tree HEAD d1e8177, pre-fix -- typed_expr.cheng return-side fact never consumed so return<call> is emitted twice, once via return statement useExpr registration and once via the generic fact-table call-statement fallback) rc=2 (gCallCount double-incremented, i.e. helper() physically called twice for one logical call); fixed DRV (scratchpad/chain30/ignite_20260715T0112/DRV, T52 v3.1 fix landed in src/core/lang/typed_expr.cheng TypedExprIrAppendStatementsForScopeWithFactsAndExprLayer) rc=1 (matches oracle). Rescued from diag_T52/repro_rescue/t52_repro1.cheng (diag_T52/README.md v3 repro1). This is the canonical red->green repro for the T52 double-exec bug: if-guarded bridge() tail-calls helper(pid, gLast) at return position; helper increments a global counter so double physical execution is directly observable via exit code."
  },
  {
    "name": "t52b-return-call-noif",
    "tags": [
      "family:t52-return-call-fact-dedup",
      "kind:red-green-repro"
    ],
    "fixture": "t52b_return_call_noif.cheng",
    "expectRc": 1,
    "note": "as-measured (2026-07-15) three-way: oracle cheng.stage3 rc=1 (correct); defective DRV (scratchpad/chain24/ignite_20260714T1600/DRV, d1e8177) rc=2 (double-exec, same bug as t52a); fixed DRV (scratchpad/chain30/ignite_20260715T0112/DRV) rc=1 (matches oracle). Rescued from diag_T52/repro_rescue/t52_repro3_noif.cheng (diag_T52/README.md v3 repro3_noif). Single-statement function body (no if branch) proves the double-exec trigger is purely about the return-root call fact being left unconsumed, independent of body shape/branching -- narrower variant of t52a with the guard clause removed."
  },
  {
    "name": "t52c-return-binop-embedded-call",
    "tags": [
      "family:t52-return-call-fact-dedup",
      "kind:regression-lock"
    ],
    "fixture": "t52c_return_binop_embedded_call.cheng",
    "expectRc": 1,
    "note": "as-measured (2026-07-15) three-way: oracle cheng.stage3 rc=1, defective DRV (chain24, d1e8177) rc=1, fixed DRV (chain30) rc=1 -- all three agree, this fixture does NOT distinguish oracle/defective/fixed on the DRV pair available in this session. Re-derived (no rescued source file existed for this exact shape) from diag_T52/README.md v3.1 case1_binop description of the real production bug site backend2FragCodecStrSize (return 4 + 4 + strings.Len(s)); helper() with a side-effect counter substitutes for strings.Len (a pure call) so double-exec would be observable if it recurred, but it does not on the DRVs tested here. Recorded honestly as a coverage/regression-lock fixture for the return<binop-embedding-a-call> shape (guards against the v3.1-fixed over-consumption bug where the naive v3 patch mistook an embedded call for the return statement root value and wrongly suppressed its fact), not as a reproduced red->green case."
  },
  {
    "name": "t52d-return-fieldarg",
    "tags": [
      "family:t52-return-call-fact-dedup",
      "kind:regression-lock"
    ],
    "fixture": "t52d_return_fieldarg.cheng",
    "expectRc": 7,
    "note": "as-measured (2026-07-15) three-way: oracle cheng.stage3 rc=7, defective DRV (chain24, d1e8177) rc=7, fixed DRV (chain30) rc=7 -- all three agree, no distinguishing power (add2(p.a,p.b) is pure, a double-call would be silently unobservable via exit code). Rescued from diag_T52/repro_rescue/repro_fieldarg.cheng. Recorded as a regression-lock coverage fixture for the return<call-with-struct-field-args> shape adjacent to the t52a/b family, not a reproduced defect."
  },
  {
    "name": "t52e-return-cstring-mirror",
    "tags": [
      "family:t52-return-call-fact-dedup",
      "kind:regression-lock"
    ],
    "fixture": "t52e_return_cstring_mirror.cheng",
    "expectRc": 7,
    "note": "as-measured (2026-07-15) three-way: oracle cheng.stage3 rc=7, defective DRV (chain24, d1e8177) rc=7, fixed DRV (chain30) rc=7 -- all three agree, no distinguishing power (inner/outer are pure cstring/nil-guard passthroughs). Rescued from diag_T52/repro_rescue/repro_cstr2.cheng, mirroring the cheng_str_param_to_cstring_compat family named in diag_T52/README.md v3.1 as an independently-existing missing_call_target defect (out of scope for T52, tracked separately). Recorded as a regression-lock coverage fixture for the return<call>-through-nested-nil-guard shape, not a reproduced T52 defect."
  }
]
