import std/strings as strings

fn main(): int32 =
    let cur: str = "line0\n"
    let n: int32 = 1
    let updated: str = cur + "line" + strings.IntToStr(n) + "\n"
    if strings.Len(updated) != 12:
        return 1
    return 0
