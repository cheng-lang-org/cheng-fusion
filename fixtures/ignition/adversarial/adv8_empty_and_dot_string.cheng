type
    Rec =
        empty: str
        dot: str

fn main(): int32 =
    var r = Rec(empty: "", dot: ".")
    if len(r.empty) != 0:
        return 1
    if len(r.dot) != 1:
        return 2
    return 7
