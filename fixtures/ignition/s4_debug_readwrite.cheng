import std/os as os
import std/strings as strings

fn main(): int32 =
    let path: str = "/tmp/bisect11/s4_debug_scratch.txt"
    os.WriteFile(path, "hello")
    let back: str = os.ReadFile(path)
    return strings.Len(back)
